#include "main.h"
/**
*_abs - Function calculates the value of an int
*@n: The function takes a single argument int n
*Return: The generated absolute value
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}

